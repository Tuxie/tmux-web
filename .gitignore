node_modules/
dist/
build/
tmux-web
index.js.map
test-results/
playwright-report/
tmp/
.worktrees/
.codex
.claude/
*.swp
coverage/

# Generated by scripts/generate-assets.ts before every compile + test
# run. `// @ts-nocheck` + tsconfig-excluded, so tooling already treats
# it as machine output. Committing produced diff noise paired with
# every theme/asset edit; CI regenerates in the build step anyway.
src/server/assets-embedded.ts

fonts/**
!fonts/*.woff2

# Mempalace files.
mempalace.yaml
entities.json
