{
  "name": "tmux-web",
  "version": "1.9.0",
  "type": "module",
  "private": true,
  "license": "ISC",
  "description": "Browser-based tmux frontend with embedded xterm.js terminal",
  "main": "src/server/index.ts",
  "scripts": {
    "dev": "bun run bun-build.ts --watch & bun run --watch src/server/index.ts",
    "build": "bun run bun-build.ts",
    "test": "bun test && bunx playwright test",
    "test:unit": "bun test",
    "test:e2e": "bunx playwright test",
    "coverage": "bun test --coverage --coverage-reporter=lcov --coverage-reporter=text",
    "coverage:check": "bun test --coverage --coverage-reporter=lcov && bun run scripts/check-coverage.ts",
    "bench": "bun run scripts/bench-render-math.ts",
    "bench:check": "bun run scripts/bench-render-math.ts --json | bun run scripts/bench-compare.ts bench/baseline.json -",
    "start": "bun src/server/index.ts",
    "desktop:dev": "bun run scripts/build-desktop-prereqs.ts && electrobun dev",
    "desktop:build": "electrobun build",
    "desktop:stable": "bun run scripts/build-desktop-prereqs.ts && electrobun build --env=stable"
  },
  "dependencies": {
    "@noble/hashes": "^2.2.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.59.1",
    "@types/bun": "^1.3.13",
    "@types/node": "^25.6.0",
    "electrobun": "1.16.0",
    "fast-check": "^4.7.0",
    "jsdom": "^29.0.2",
    "pngjs": "^7.0.0",
    "typescript": "^6.0.3"
  }
}
