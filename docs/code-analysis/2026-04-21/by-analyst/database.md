# Database Analyst

_Not applicable — no database surface in this project. The scout's applicability flag for `database` is `absent` (no schema, no migrations, no ORM, no query-builder usage). Per-session settings persist to a single file (`~/.config/tmux-web/sessions.json`) with atomic `.part` → rename writes; nothing in the project looks or behaves like a database workload._
