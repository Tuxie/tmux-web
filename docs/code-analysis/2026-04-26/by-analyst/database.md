# Database Analyst

_Not applicable — Scout flag `database: absent`. The project has no SQL, no migrations, no ORM imports; persistence is JSON file (`src/server/sessions-store.ts`) with `.part` → rename atomic writes._
